." Generated by `make man` (urfave/cli-docs ToManWithSection); do not edit by hand.
." Regenerate with: fuse.briefs --generate-man-page

.nh
.TH fuse.briefs 8

.SH NAME
.PP
fuse.briefs - Mount a BrieFS filesystem image via FUSE


.SH SYNOPSIS
.PP
fuse.briefs

.PP
.RS

.nf
[--debug|-d]
[--help|-h]
[--image|-i]=[value]
[--mount-opts]=[value]
[--mountpoint|-m]=[value]
[--version|-v]

.fi
.RE


.SH DESCRIPTION
.PP
Mount a BrieFS filesystem image as a FUSE filesystem.  The bridge is read-write with full kernel parity: all directory and file operations, extended attributes, chattr/fileattr, renameat2 (EXCHANGE/WHITEOUT), fallocate, setattr, and killpriv.  It ports the kernel journal to Go and replays it on mount, so FUSE-written volumes are crash-consistent and kernel-mountable.  The bridge is experimental; see xfstests-fuse-status.md for the xfstests pass/fail record.

.PP
\fBUsage\fP:

.PP
.RS

.nf
fuse.briefs [global options]

.fi
.RE


.SH GLOBAL OPTIONS
.PP
\fB--debug, -d\fP: enable FUSE debug output

.PP
\fB--help, -h\fP: show help

.PP
\fB--image, -i\fP="": filesystem image file or block device

.PP
\fB--mount-opts\fP="": comma-separated mount options to apply to the FUSE mount (as forwarded by mount.fuse.briefs from \fB\fCmount -t fuse.briefs -o <opts>\fR); [no]suid/[no]dev/[no]exec override the kernel-parity defaults, anything unrecognized is passed to the kernel mount, which rejects it

.PP
\fB--mountpoint, -m\fP="": mount point directory

.PP
\fB--version, -v\fP: print the version
